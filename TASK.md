# Role & Objective
You are an expert Frontend Engineer specializing in modern, lightweight, semantic static web design. Your goal is to rewrite and organize the provided legacy HTML/CSS static page markup into a clean, modern, and production-ready structure.

---

# Core Principles to Apply
Please refactor the code according to the following industry-standard static page principles:

1. **Semantic HTML5:** Replace generic layout `<div>` elements with descriptive tags (`<header>`, `<nav>`, `<main>`, `<section>`, `<article>`, `<footer>`).
2. **Modern & Responsive CSS:**
    - Use Flexbox or CSS Grid for layouts instead of floats, tables, or positioning hacks.
    - Use CSS variables (custom properties) for the color palette, typography, and spacing to ensure consistency.
    - Ensure the layout is fluid and works seamlessly on mobile devices using modern CSS (e.g., `max-width`, `clamp()`, or media queries).
3. **Accessibility (a11y):** Ensure proper heading hierarchies (`<h1>` down to `<h6>`), add descriptive `alt` tags to images, and verify interactive elements have clear focus states.
4. **Performance & Cleanliness:** Eliminate redundant or inline styles, remove dead code, and keep CSS lean for fast loading times.

---

# Instructions
1. Review the raw legacy code provided below.
2. Separate the structure (HTML) and the styling (CSS) cleanly. If keeping it in a single file, place the CSS inside a clean `<style>` block in the `<head>`.
3. Provide the final, complete, and fully refactored code block ready to be saved.
4. Do not write a long essay explaining the changes; just provide the clean code and a few brief bullet points detailing what was optimized.

---

# Legacy Code to Update